import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App.tsx'

ReactDOM.createRoot(document.getElementById('dashboardRoot')!).render(
  <React.StrictMode>
      <App />
  </React.StrictMode>,
)
